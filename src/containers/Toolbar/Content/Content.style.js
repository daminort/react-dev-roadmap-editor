import styled from 'styled-components';

export const Wrapper = styled.div`
  width: 100%;

  .p-inputtext {
    width: 100%;
    margin-bottom: 8px;
    font-size: inherit;
  }

  .p-button {
    font-size: inherit;
  }
`;
